# 【美味しい1杯】導入手順ガイド（会員認証システム版）

## 全体構成

```
【フロントエンド】               【バックエンド】
  GitHub Pages         →  fetch  →  Google Apps Script (GAS)
   index.html                              ↓
   legal/terms.html                 Googleスプレッドシート
                                     （users / members / favorites）
```

主な変更点（旧バージョンとの違い）：
- 投稿ごとの「編集用パスワード」を廃止し、**メールアドレスでの会員登録＋ログイン**に変更
- ログイン中の本人のみ、自分の投稿を編集・削除可能
- 初回ログイン時は仮パスワードから新しいパスワードへの変更が必須
- ニックネーム・パスワードの変更、パスワード再発行（仮パスワード再送）、退会（アカウント削除）に対応
- お気に入り保存、X/LINEへのシェアボタンを追加
- **お店の写真をGoogleドライブにアップロード可能**（ブラウザ側で自動的に縮小してから送信）
- **お店のURL**を投稿に追加可能
- **他の会員によるレビュー**（5段階評価＋コメント）を投稿でき、平均評価がカードに表示される
- 投稿の修正・削除に加え、自分が書いたレビューの修正・削除にも対応
- **おすすめベスト100**：会員が自分の好きな投稿を1位〜100位で順位付けして登録。1位=100点、100位=1点（101-順位）として全会員分を集計し、各投稿の「偏差値」をカードに表示

admin.html（管理者ページ）は変更不要でそのまま使えます。

---

## Step 1：Googleスプレッドシートを準備する

1. [Google スプレッドシート](https://sheets.google.com) で新しいスプレッドシートを作成します。
2. URLからスプレッドシートIDをコピーします。

```
https://docs.google.com/spreadsheets/d/【ここがID】/edit
```

> ✅ シート（users / members / favorites / reviews）はGASが自動作成します。手動でシートを作る必要はありません。
> ⚠️ 既存の「members」シートがある場合は、列構成が変わる（storeName・storeUrl・photoUrl列が追加）ため、**投稿データを一度リセット（全行削除、ヘッダー行のみ残す）**してから移行してください。すでに写真/URL機能まで導入済みで投稿データを残したい場合は、H列に `storeName` を手動で挿入し、既存のstoreUrl・photoUrl・日時の各列を1列ずつ右にずらしてください。

---

## Step 2：Google Apps Script を設定・デプロイする

1. スプレッドシートの「拡張機能」→「Apps Script」を開きます。
2. 既存のコードをすべて削除し、`code.gs` の内容を丸ごと貼り付けます。
3. **コードは書き換えません。** 代わりに、GASエディタ左側の歯車アイコン「プロジェクトの設定」→「スクリプトプロパティ」で、以下3つを追加してください。

| プロパティ名 | 値 |
|---|---|
| `SPREADSHEET_ID` | Step 1でコピーしたスプレッドシートID |
| `ADMIN_PASSWORD` | 管理者ページ用パスワード |
| `PEPPER` | パスワードハッシュ用のランダムな秘密文字列 |

> 🔒 **なぜコードに直書きしないのか**：コードファイル（code.gs）はclasp等でGitHubにpush・公開される可能性がありますが、スクリプトプロパティはGASプロジェクト内部にのみ保存され、コードファイルには一切現れません。秘密情報はここに置くのが安全です。
>
> `PEPPER`は他のアプリと使い回さない、推測されにくいランダム文字列にしてください（一度決めたら変更するとログイン中のユーザー全員がパスワード再設定必要になります）。
>
> 写真保存先フォルダを指定したい場合は、任意で `PHOTO_FOLDER_ID` というプロパティも追加できます（未設定なら自動でフォルダが作成されます）。

4. Ctrl+S で保存。
5. 「デプロイ」→「新しいデプロイ」→ 種類「ウェブアプリ」を選択。
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**
6. デプロイ後に表示される「ウェブアプリのURL」をコピーします。

```
https://script.google.com/macros/s/XXXXXXXXXXXX/exec
```

> ⚠️ コードを修正するたびに**新しいデプロイ**を作成してください（既存デプロイの更新だけでは反映されません）。旧バージョンのURLを使い続ける場合は「デプロイを管理」→「編集」→「新バージョン」でも構いません。

### 写真保存用フォルダについて
`PHOTO_FOLDER_ID` は未設定のままでOKです。未設定の場合、GAS実行アカウントのGoogleドライブ直下に自動で `oishii-ippai-photos` フォルダが作成され、そこに写真が保存されます。特定のフォルダを使いたい場合はそのフォルダIDをスクリプトプロパティに追加してください。

アップロードされた写真は「リンクを知っている全員が閲覧可」で共有設定されます（サイト上に画像として表示するため）。ドライブの空き容量にご注意ください。

### メール送信について
`MailApp.sendEmail` を使って仮パスワードを送信します。**GASを実行するGoogleアカウントの送信元**からメールが届きます。個人アカウントは1日100通までの送信上限があるのでご注意ください。

---

## Step 3：フロントエンドにURLを設定する

`index.html` 内の以下の行をStep 2でコピーしたURLに書き換えます（admin.htmlにも同様の行があるので、そちらも同じURLに揃えてください）。

```javascript
const GAS_URL = 'YOUR_GAS_WEB_APP_URL_HERE';
```

---

## Step 4：ファイル構成とGitHub Pagesでの公開

以下の構成でリポジトリに配置してください。

```
/
├── index.html
├── admin.html
└── legal/
    └── terms.html
```

1. GitHubリポジトリにこれらのファイルをアップロード（またはコミット＆プッシュ）。
2. 「Settings」→「Pages」で公開設定（Branch: main、フォルダ: /root）。
3. 公開URL：`https://【ユーザー名】.github.io/【リポジトリ名】/`

---

## 動作確認チェックリスト

- [ ] トップページ右上に「ログイン / 新規会員登録」ボタンが表示される
- [ ] 新規会員登録 → 利用規約に同意しないと登録できない
- [ ] 登録後、指定したメールアドレスに仮パスワードが届く
- [ ] 仮パスワードでログイン → 強制的にパスワード変更モーダルが表示される（閉じるボタンなし）
- [ ] パスワード変更後、通常通りログイン状態になる
- [ ] 「✏️ 投稿する」タブ → ログイン中なら投稿フォームが表示される
- [ ] 投稿後、一覧に「あなたの投稿」バッジ付きで表示される
- [ ] 自分の投稿にのみ「修正」「削除」ボタンが表示される
- [ ] 他人の投稿には修正・削除ボタンが出ない
- [ ] お気に入り（☆）をクリックして★に変わる、再読み込みしても保持される
- [ ] X / LINE のシェアボタンが動作する
- [ ] マイページからニックネーム変更ができる
- [ ] マイページからパスワード変更ができる（現在のパスワードが必要）
- [ ] 「パスワードをお忘れですか？」から仮パスワードが再送される
- [ ] マイページから退会すると、投稿・お気に入りも削除されログアウトされる
- [ ] admin.html（管理者ページ）が引き続き正常に動作する
- [ ] 投稿フォームで写真を選択すると自動的に縮小され、プレビューが表示される
- [ ] 大きいサイズの写真（数MB）を選んでもエラーにならず登録できる
- [ ] お店のURLを入力すると、カードに「🔗 お店のページを見る」リンクが表示される
- [ ] 「レビューを見る」を開き、星評価＋コメントを投稿できる
- [ ] 投稿するとカードの平均評価（★の数値と件数）が更新される
- [ ] 自分が書いたレビューだけ「修正」「削除」が表示される
- [ ] 投稿を削除すると、紐づく写真（ドライブ）とレビューも一緒に削除される

---

## スプレッドシートの列構成（参考）

**users**

| 列 | 内容 |
|----|------|
| A | userId |
| B | email |
| C | passwordHash |
| D | salt |
| E | nickname |
| F | isTempPassword |
| G | agreedTermsAt |
| H | status（active / deleted） |
| I | createdAt |

**members**

| 列 | 内容 |
|----|------|
| A | rowId |
| B | userId |
| C | nickname |
| D | contact |
| E | area |
| F | drink |
| G | comment |
| H | storeName |
| I | storeUrl |
| J | photoUrl |
| K | date |

**favorites**

| 列 | 内容 |
|----|------|
| A | userId |
| B | rowId |
| C | createdAt |

**reviews**

| 列 | 内容 |
|----|------|
| A | reviewId |
| B | rowId（対象の投稿） |
| C | userId |
| D | nickname |
| E | rating（1〜5） |
| F | comment |
| G | createdAt |
| H | updatedAt |

> 1会員につき1投稿への評価は1件まで。再度投稿すると上書き更新されます。

**rankings**（おすすめベスト100）

| 列 | 内容 |
|----|------|
| A | userId |
| B | rowId（ランクインした投稿） |
| C | rank（1〜100。1位が最上位） |
| D | updatedAt |

> 得点は保存せず、集計時に `101 - rank` で都度計算します（1位=100点、100位=1点）。1会員のベスト100は保存のたびに全件洗い替えされます。偏差値は「誰か1人以上のベスト100にランクインした投稿」だけを母集団として、平均50・標準偏差10になるよう計算しています。

---

## セキュリティについて

| 対策 | 内容 |
|------|------|
| 秘密情報の管理 | SPREADSHEET_ID・ADMIN_PASSWORD・PEPPERはコードに書かず、GASのスクリプトプロパティに保存（GitHub等にコードをpushしても漏れない） |
| パスワードのハッシュ化 | SHA-256 + ユーザーごとのsalt + 共通pepper、2000回ストレッチ |
| セッション管理 | CacheServiceによるトークン方式（有効期限6時間） |
| 本人確認 | 投稿の編集・削除はセッションのuserIdと投稿のuserIdが一致する場合のみ許可 |
| 退会時の情報削除 | メールアドレス・パスワードハッシュを消去、投稿・お気に入りも削除 |
| XSS対策 | フロントはescHtml()、GASはsanitize()でエスケープ |

> ⚠️ これは簡易的な実装です。医療・金融など機密性の高い用途には使用しないでください。
