# 【美味しい1杯】導入手順ガイド

## 全体構成

```
【フロントエンド】               【バックエンド】
  GitHub Pages         →  fetch  →  Google Apps Script (GAS)
   index.html                              ↓
                                    Googleスプレッドシート
                                    （データベース）
```

---

## Step 1：Googleスプレッドシートを準備する

1. [Google スプレッドシート](https://sheets.google.com) を開き、**新しいスプレッドシートを作成**します。
2. スプレッドシートの名前を任意に設定します（例：「飲み友達DB」）。
3. シート名（タブ名）を **`members`** に変更します（デフォルトは「シート1」）。
4. **URLからスプレッドシートIDをコピー**します。

```
https://docs.google.com/spreadsheets/d/【ここがID】/edit
```

> ✅ ヘッダー行の追加は不要です。GASが自動で作成します。

---

## Step 2：Google Apps Script を設定・デプロイする

### 2-1. GASエディタを開く

1. スプレッドシートのメニューから **「拡張機能」→「Apps Script」** をクリックします。
2. GASエディタが開きます。

### 2-2. コードを貼り付ける

1. エディタに最初から入っているコードを**すべて削除**します。
2. `code.gs` の内容を**丸ごとコピー＆ペースト**します。
3. コード先頭の以下の行を、**あなたのスプレッドシートID**に書き換えます。

```javascript
var SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID_HERE';
//  ↑ Step 1 でコピーしたIDに変更する
```

4. **Ctrl+S（Mac: ⌘+S）** で保存します。

### 2-3. ウェブアプリとしてデプロイする

1. 右上の **「デプロイ」→「新しいデプロイ」** をクリックします。
2. 「種類の選択」で **「ウェブアプリ」** を選択します。
3. 以下のように設定します。

| 項目 | 設定値 |
|------|--------|
| 説明 | 飲み友達API（任意） |
| 次のユーザーとして実行 | **自分** |
| アクセスできるユーザー | **全員** |

4. **「デプロイ」** をクリックします。
5. 初回は**権限の承認**を求められます。「アクセスを承認」→ Googleアカウントを選択 → 「詳細」→「安全ではないページに移動」→「許可」と進めてください。
6. デプロイ完了後、**「ウェブアプリのURL」**が表示されます。このURLをコピーしておきます。

```
https://script.google.com/macros/s/XXXXXXXXXXXX/exec  ← これをコピー
```

> ⚠️ コードを修正したら、必ず「デプロイを管理」→「編集」→「新バージョン」で**再デプロイ**が必要です。

---

## Step 3：フロントエンドにURLを設定する

`index.html` の以下の行を探して、GASのURLに書き換えます。

```javascript
// =============================================
// ⚙️ 設定：GASウェブアプリのURLをここに入力
// =============================================
const GAS_URL = 'YOUR_GAS_WEB_APP_URL_HERE';
//               ↑ Step 2-3 でコピーしたURLに変更する
```

---

## Step 4：GitHub Pages で公開する

1. GitHubで**新しいリポジトリ**を作成します（例：`nomitomo`）。
2. `index.html` をリポジトリにアップロードします。
3. リポジトリの **「Settings」→「Pages」** を開きます。
4. 「Branch」を `main`、フォルダを `/ (root)` に設定して **「Save」** をクリックします。
5. 数分後、以下のようなURLでサイトが公開されます。

```
https://【あなたのGitHubユーザー名】.github.io/nomitomo/
```

---

## 動作確認

公開URLにアクセスして以下を確認してください：

- [ ] ページが表示される
- [ ] 「新規登録」からプロフィールを登録できる
- [ ] 「メンバー一覧」に登録データが表示される
- [ ] 「修正」ボタン → 正しいパスワードで編集できる
- [ ] 「修正」ボタン → 間違ったパスワードでエラーになる
- [ ] 「削除」ボタン → 正しいパスワードで削除できる
- [ ] Googleスプレッドシートにデータが反映されている

---

## スプレッドシートの列構成（参考）

GASが自動でヘッダーを作成します。

| 列 | 内容 |
|----|------|
| A | rowId（自動生成UUID） |
| B | nickname（ニックネーム） |
| C | contact（連絡先/SNS） |
| D | area（よく行くエリア） |
| E | drink（お酒の好み） |
| F | comment（一言コメント） |
| G | passwordHash（SHA-256ハッシュ） |
| H | date（登録日時） |

> 🔒 パスワードは**平文では保存されず、SHA-256ハッシュ値**で保存されます。スプレッドシートを見ても元のパスワードはわかりません。

---

## よくあるトラブル

### 「データを取得できませんでした」と表示される

- GASのURLが正しいか確認してください（`YOUR_GAS_WEB_APP_URL_HERE` のまま残っていませんか？）
- GASが「全員」アクセス可能でデプロイされているか確認してください
- コードを修正した場合は**再デプロイ**が必要です

### 登録・削除・修正が反映されない

- 「デプロイを管理」から「新バージョンでデプロイ」しているか確認してください
- スプレッドシートのシート名が `members` になっているか確認してください

### CORS エラーが出る

- GASのデプロイ設定で「アクセスできるユーザー」が**「全員」**になっているか確認してください
- GASの `doPost` が正しく定義されているか確認してください

---

## セキュリティについて

| 対策 | 内容 |
|------|------|
| パスワードのハッシュ化 | SHA-256でハッシュ化してDB保存。平文は残りません |
| 本人確認 | 修正・削除時にパスワードをGAS側で検証 |
| XSS対策 | フロントはescHtml()でエスケープ、GASはsanitize()で処理 |
| APIキー不要 | フロントにはGAS URLのみ（DB IDやパスワードは含まない） |

> ⚠️ これは簡易的な実装です。医療・金融など機密性の高い用途には使用しないでください。
